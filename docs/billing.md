# Billing architecture

Added column `billing_mode` to users to define internal or external billing, to define what the `active` status of the user is tied to: Plans paid in app or teacher controlling it manually. 

